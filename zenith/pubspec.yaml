name: zenith
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  cupertino_icons: any
  hive: any
  hive_flutter: any
  provider: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: any
  build_runner: any
  flutter_lints: any

flutter:
  uses-material-design: true
